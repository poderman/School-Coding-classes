# Add your Python code here. E.g.
from microbit import *
from motobit import MotoBit 
import radio

moto = MotoBit() 
moto.enable()

radio.on()

pin16.set_analog_period(90)
turn = 90

while True:
    
    incoming = radio.receive()
    if incoming == 'super':
        display.show("S")
        moto.drive(speed_left = 100, speed_right = 100)
        
    if incoming == 'left':
        display.show("L")
        #turn = turn + 1
        pin15.write_analog(150)
        sleep(100)
    elif incoming == 'right':
        display.show("R")
        #turn = turn + 1
        pin15.write_analog(10)
        sleep(100)
        
        
    elif incoming == 'up':
        display.show("U")
        moto.drive(speed_left = 100, speed_right = 100)
        
        
    elif incoming == 'down':
        display.show("D")
        moto.drive(speed_left = -100, speed_right = -100)
        
    else:
        print("Stoped")
        
        
        
 ///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
 
 
 # Add your Python code here. E.g.
from microbit import *
import radio

radio.on()

while True:
    gesture = accelerometer.current_gesture()
    
    #if pin0.read_digital(1)
    #    display.show("S")
    #    radio.send('super')
    
    if button_a.is_pressed():
        display.show("L")
        radio.send('left')
        
    elif button_b.is_pressed():
        display.show("R")
        radio.send('right')
        
    elif gesture == "face up":
        display.show("U")
        radio.send('up')
        
    elif gesture == "up":
        display.show("D")
        radio.send('down')
        
    else:
        display.show(Image.HEART)


        
        
        
